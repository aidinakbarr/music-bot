services:
  - type: web
    name: music-bot
    env: python
    runtime: python
    region: oregon
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python src/main.py"
    pythonVersion: 3.11.9
    autoDeploy: true
